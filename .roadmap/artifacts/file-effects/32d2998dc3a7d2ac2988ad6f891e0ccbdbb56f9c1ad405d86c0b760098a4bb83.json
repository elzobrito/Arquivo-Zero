{
  "after_sha256": "4d47c4081329bc6dbab8e989b0c317f818e6f0a2a0016a78e115e73251fd45ba",
  "artifact_id": "ART-4d47c4081329bc6dbab8e989b0c317f818e6f0a2a0016a78e115e73251fd45ba",
  "artifact_sha256": "32d2998dc3a7d2ac2988ad6f891e0ccbdbb56f9c1ad405d86c0b760098a4bb83",
  "before_sha256": "4d47c4081329bc6dbab8e989b0c317f818e6f0a2a0016a78e115e73251fd45ba",
  "bytes": 2481,
  "content": "\"use strict\";\n\n/**\n * Deterministic filters/sort for evidence view-models.\n * Never mutates input arrays or records.\n */\n\nconst DEFAULT_FILTERS = {\n  query: \"\",\n  tag: \"all\",\n  admissibility: \"all\",\n  integrity: \"all\",\n  quality: \"all\",\n  support: \"all\",\n};\n\nfunction normalizeQuery(q) {\n  return String(q || \"\").trim().toLowerCase();\n}\n\nfunction matchesQuality(model, band) {\n  if (band === \"all\") return true;\n  if (band === \"unknown\") return model.quality == null;\n  const q = model.quality;\n  if (q == null) return false;\n  if (band === \"high\") return q >= 0.8;\n  if (band === \"medium\") return q >= 0.5 && q < 0.8;\n  if (band === \"low\") return q < 0.5;\n  return true;\n}\n\nfunction applyFilters(models, filters) {\n  const f = Object.assign({}, DEFAULT_FILTERS, filters || {});\n  const query = normalizeQuery(f.query);\n  return (models || []).filter(function (m) {\n    if (!m) return false;\n    if (f.tag !== \"all\" && m.tag !== f.tag) return false;\n    if (f.admissibility !== \"all\" && m.admissibility !== f.admissibility) return false;\n    if (f.integrity !== \"all\" && m.integrity !== f.integrity) return false;\n    if (!matchesQuality(m, f.quality)) return false;\n    if (f.support === \"yes\" && !m.canSupportArrest) return false;\n    if (f.support === \"no\" && m.canSupportArrest) return false;\n    if (query) {\n      const hay = [\n        m.id, m.title, m.tag, m.fact, m.interpretationLabel, m.source, m.method,\n      ].join(\" \").toLowerCase();\n      if (hay.indexOf(query) === -1) return false;\n    }\n    return true;\n  });\n}\n\nfunction applySort(models, sort) {\n  const key = (sort && sort.key) || \"id\";\n  const dir = (sort && sort.dir) === \"desc\" ? -1 : 1;\n  const copy = (models || []).slice();\n  copy.sort(function (a, b) {\n    const av = a && a[key] != null ? String(a[key]) : \"\";\n    const bv = b && b[key] != null ? String(b[key]) : \"\";\n    if (av < bv) return -1 * dir;\n    if (av > bv) return 1 * dir;\n    // stable tie-break by id\n    const ai = a && a.id != null ? String(a.id) : \"\";\n    const bi = b && b.id != null ? String(b.id) : \"\";\n    if (ai < bi) return -1;\n    if (ai > bi) return 1;\n    return 0;\n  });\n  return copy;\n}\n\nfunction uniqueTags(models) {\n  const seen = {};\n  const out = [];\n  (models || []).forEach(function (m) {\n    if (!m || !m.tag || seen[m.tag]) return;\n    seen[m.tag] = true;\n    out.push(m.tag);\n  });\n  out.sort();\n  return out;\n}\n\nmodule.exports = {\n  DEFAULT_FILTERS,\n  applyFilters,\n  applySort,\n  uniqueTags,\n  matchesQuality,\n};\n",
  "effect_scope": "workspace",
  "encoding": "utf-8",
  "path": "src/ui/evidence/evidence-filters.js",
  "source_path": "src/ui/evidence/evidence-filters.js",
  "ts": "2026-09-17T02:29:23Z"
}
